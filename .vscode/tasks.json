{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "CMake: Build Debug",
      "type": "shell",
      "command": "cmake --build ./build --target GAME_APPLICATION -j6",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "clear": true // Clear the terminal on build
      }
    }
  ]
}