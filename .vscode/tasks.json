{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake: Build Debug",
            "type": "shell",
            "command": "cmake --build ./build --target GAME_APPLICATION -j6",  
            "group": "build"
        },
    ]
}